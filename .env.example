# API Configuration
# Thay đổi URL này theo môi trường của bạn
VITE_API_BASE_URL=https://localhost:7151/api
VITE_API_TIMEOUT=5000

# Feature Flags
# Set true để dùng mock data, false để dùng API thật
VITE_USE_MOCK_DATA=false

# Cloudinary Configuration (for image uploads)
# Sign up at https://cloudinary.com to get these values
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=unsigned_preset

# Note: Create an unsigned upload preset in Cloudinary dashboard:
# 1. Go to Settings > Upload
# 2. Scroll to "Upload presets"
# 3. Click "Add upload preset"
# 4. Set "Signing Mode" to "Unsigned"
# 5. Configure folder and other settings as needed
# 6. Copy the preset name to VITE_CLOUDINARY_UPLOAD_PRESET

# Authentication (sẽ dùng sau này)
# VITE_AUTH_TOKEN_KEY=fptech_auth_token
